#ifndef __GAME_OVER__
#define __GAME_OVER__

/*
    Biblioteca definida para atualizacao da tela 
    de game over do jogo blocks
    Desenvolvido por Pedro Amaral Chapelin
*/

// Funcao que atualiza a tela de Game over de acordo com a pontuacao obtida
void updateGame_Over(int mouseX, int mouseY, int *ms, State_t* states, Sounds_t* sounds);

#endif